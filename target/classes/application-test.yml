spring:
  security:
    user:
      name: ${BASIC_USER:admin}
      password: ${BASIC_PASS:admin123}

management:
  endpoints:
    web:
      exposure:
        include: "health,info"

order:
  paging:
    defaultPageSize: ${DEFAULT_PAGE_SIZE:10}
    maxPageSize: ${MAX_PAGE_SIZE:50}
